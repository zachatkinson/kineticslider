/**
 * Internal types used by hooks but needed for component parameters
 */
import type { AnimationConfig, AnimationOptions } from './animation';
import type { GestureConfig, GestureOptions, SwipeDirection } from './gestures';

export type {
  AnimationOptions,
  AnimationConfig,
  SwipeDirection,
  GestureOptions,
  GestureConfig,
};
