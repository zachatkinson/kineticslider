// Export the main component
export { default as KineticSlider } from './KineticSlider';

// Export component types
export type { KineticSliderProps } from './types';

// Export filter types and factory if consumers need to customize filters
export { FilterFactory } from './filters';
export type { FilterConfig, FilterType } from './filters/types';